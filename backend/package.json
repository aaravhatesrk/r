{
  "name": "rooted-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend proxy for Rooted's Wellness Advisor and PitchIQ's Coach AI / live match lookup. Holds the Gemini and football-data.org API keys server-side so they never reach client JS.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "express-rate-limit": "^7.4.1"
  }
}
