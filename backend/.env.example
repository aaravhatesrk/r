# Copy to .env for local dev. In production (Render), set these as real
# environment variables in the service dashboard — never commit real values.
GEMINI_API_KEY=
FOOTBALL_DATA_API_KEY=
ALLOWED_ORIGIN=https://rooted-i4d3.onrender.com
PORT=3001
